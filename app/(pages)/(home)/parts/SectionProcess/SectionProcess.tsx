import styles from "./SectionProcess.module.scss";

const SectionProcess = () => {
	return (
		<div className={styles.sectionProcessContainer}>
			<p>This is the SectionProcess</p>
		</div>
	);
};
export default SectionProcess;
